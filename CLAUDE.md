# PCCP Java 학습 프로젝트

## 목표
- PCCP Lv.3 이상 취득 가능한 코딩 역량 확보
- 언어: Java
- 플랫폼: 프로그래머스

## 학습 플랜 (12주)
- 노션 원본: 헤밀레라이프랩 / PCCP Java 학습 플랜 (12주)
- 시작일: 2026-04-01 (수요일)
- 방식: 매일 1문제 (30분~1시간), 30분 고민 → 못 풀면 풀이 확인 → 다음 날 복습

### Phase 1: 기초 체력 (1~3주차) - Lv.1
- 1주차: 문자열 · 배열 · 연결리스트 (번갈아 풀기)
- 2주차: 조건문 · 반복문 · 함수
- 3주차: 해시(HashMap) 기초

### Phase 2: 핵심 자료구조 · 알고리즘 (4~7주차) - Lv.2
- 4주차: 정렬 · 그리디
- 5주차: 스택 · 큐 · 덱
- 6주차: DFS · BFS
- 7주차: 이진 탐색

### Phase 3: 심화 알고리즘 (8~10주차) - Lv.2~3
- 8주차: 그래프 · 트리
- 9주차: 힙(PriorityQueue)
- 10주차: DP (동적 프로그래밍)

### Phase 4: 실전 모의고사 (11~12주차)
- 11주차: PCCP 기출 풀이
- 12주차: 약점 보완 + 모의 시험

## 프로젝트 구조
- `src/weekN/` — N주차 풀이 코드
- Claude가 문제 출제 + 코드 리뷰, 주 1~2회는 프로그래머스 실전 연습 병행

## 코드 리뷰 규칙
- 풀이(정답 코드)는 사용자가 명시적으로 요청할 때만 알려줄 것
- 그 전에는 힌트, 접근 방식, 개념 설명까지만 제공

## 진행 규칙
- 문제 파일명: `Day{주차}_{YYMMDD}_{번호}.java` 형식 (예: `Day1_260401_1.java`)
- 패키지: `week1`, `week2`, ... 형식
- import는 `import java.util.*;` 와일드카드 사용
- 문제 주석에 힌트(사용할 API, 접근 방식 등)를 포함할 것 (사용자가 빼달라 할 때까지 유지)

## 문제 출제 형식
- Scanner 입출력 방식이 아닌, 프로그래머스 스타일로 출제할 것
- `solution()` 메서드에 매개변수로 입력을 받고, 결과를 return하는 형태
- `main()`에는 테스트 케이스를 넣어서 PASS/FAIL로 검증
- 사용자는 핵심 알고리즘 로직만 작성하면 되도록 구성

## Git 커밋 규칙
- **스테이징은 항상 `git add .`** 로 전체 변경 사항을 한 번에 스테이징할 것.
  파일 단위로 쪼개지 말고, 작업 중인 모든 변경을 묶어서 하나의 커밋으로 처리한다.

## 코딩 스타일 선호
- **좌표 표기**: 2차원 배열 `board[y][x]` 인덱싱과 일치시키기 위해 `(y, x)` 순서로 사고함. 변수명/파라미터/주석 모두 `y`를 먼저 쓸 것.
- **방향 배열**: 상 → 우 → 하 → 좌 순서로 고정 (시계 방향).
  ```java
  static int[] dy = {-1, 0, 1, 0};  // 상, 우, 하, 좌
  static int[] dx = {0, 1, 0, -1};
  ```
  - `R` (오른쪽 회전) = `(dir + 1) % 4`
  - `L` (왼쪽 회전) = `(dir + 3) % 4`
- **시뮬레이션 / DFS / BFS 문제**: 보드판, 방향 배열, 방문 체크(`visited`) 같은 공용 상태는 **클래스 레벨 변수(`static`)** 로 두는 스타일을 선호.
  - 단, `solution()` 진입 시점에 상태성 있는 변수(`visited`, 보드 등)는 **반드시 초기화**할 것. (테스트 케이스 간 오염 방지)
  - 불변 상수(`dy`, `dx`)는 `static final`로 선언 가능.
